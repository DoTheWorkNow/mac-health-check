---
name: mac-health-check
description: 快速检查 Mac 系统健康状况 — 内存、CPU、磁盘、电池、进程占用，并给出可操作的优化建议。当用户提到内存占用、系统卡顿、电脑慢、电脑发热、风扇响、查看系统状况、系统监控、Mac 运行状态、清理内存、为什么卡、check system health、memory usage 时触发此技能。即使用户只是随口说「电脑好慢」或「风扇在转」也应该触发。
---

# Mac Health Check

一键诊断 Mac 系统健康状况并给出建议。

## 工作流程

1. 运行采集脚本（一次 Bash 调用获取全部数据）：
```bash
bash ~/.claude/skills/mac-health-check/scripts/sysmon.sh
```

2. 解读脚本输出，用中文向用户呈现：

### 输出格式

用一个紧凑的表格 + 简短建议，不要重复原始数据：

```
## 系统概览
| 指标 | 状态 |
|------|------|
| 内存 | XX/XXGB (XX%) — [正常/偏高/紧张] |
| CPU  | XX% idle — [空闲/正常/繁忙] |
| 磁盘 | XX/XXGB — [充足/偏紧/告急] |
| 电池 | XX% [充电中/电池供电] |

## 占用大户 (Top 5)
| 进程 | 内存 | CPU | 说明 |
...

## 建议
- [可操作的建议，比如可以 kill 的残留进程、可以关闭的应用]
```

### 判断标准

- **内存压力** >80%：紧张，需要清理
- **内存压力** 60-80%：偏高，列出可优化项
- **内存压力** <60%：正常
- **Swap out** >100万：系统一直在换页，建议释放内存
- **CPU idle** <20%：繁忙
- **磁盘使用** >85%：告急
- **进程 RSS >500MB**：重点关注，判断是否合理

### 建议原则

- 只建议用户能安全执行的操作（关闭应用、kill 残留进程）
- 识别常见的内存大户：Electron 应用多开、残留 node 进程、虚拟机、IDE helper
- 如果用户问「帮我清理」，先列出要 kill 的进程让用户确认，不要直接 kill
- 简洁。不需要解释 vm_stat 的原理或内存管理机制

## Token 节省策略

这个 skill 的核心设计就是省 token：脚本在 shell 层完成所有采集和格式化，Claude 只需要一次 Bash 调用就能拿到全部数据。不要额外跑 `top`、`ps`、`vm_stat` 等命令 — 脚本已经包含了所有需要的信息。
